---
import type { Tool } from '../data/tools';
import { getReview } from '../data/reviews';
import VerifiedBadge from './VerifiedBadge.astro';
const { tool } = Astro.props as { tool: Tool };
const reviewed = Boolean(getReview(tool.slug));
---

<span class="badge badge--teal">{tool.category}</span>
<span class="badge badge--cyan">{tool.license}</span>
<span class="badge">{tool.deployment}</span>
{
  reviewed && (
    <span class="badge badge--reviewed" title="Curator review available">
      <VerifiedBadge tool={tool} size="sm" />
      <span>REVIEWED</span>
    </span>
  )
}
{
  tool.personalPick && (
    <span class="badge badge--amber" title="Personal pick">
      <svg class="pick-icon" viewBox="0 0 24 24" aria-hidden="true">
        <path d="M12 20.5S4.5 15.9 4.5 10.5A4.3 4.3 0 0 1 12 8a4.3 4.3 0 0 1 7.5 2.5c0 5.4-7.5 10-7.5 10Z" />
      </svg>
      personal pick
    </span>
  )
}
{
  tool.generalPick && (
    <span class="badge badge--cyan" title="General pick">
      <svg class="pick-icon" viewBox="0 0 24 24" aria-hidden="true">
        <path d="m5 12 4.5 4.5L19 7" />
      </svg>
      general pick
    </span>
  )
}
<style>
  .badge {
    align-items: center;
    box-sizing: border-box;
    border: 1px solid var(--color-surface-700);
    color: var(--color-surface-300);
    display: inline-flex;
    font: 12px/1.3 var(--font-mono);
    gap: 4px;
    height: 26px;
    margin: 0;
    padding: 0 7px;
    white-space: nowrap;
  }
  .badge--teal {
    color: var(--color-teal-300);
    border-color: var(--color-teal-700);
  }
  .badge--cyan {
    color: var(--color-cyan-500);
    border-color: color-mix(in srgb, var(--color-cyan-500) 60%, transparent);
  }
  .badge--amber {
    color: var(--color-amber-400);
    border-color: var(--color-amber-400);
  }
  .badge--reviewed {
    color: var(--color-teal-300);
    border-color: var(--color-teal-300);
  }
  .badge--reviewed :global(svg) {
    width: 12px;
    height: 12px;
  }
  .pick-icon {
    fill: none;
    height: 12px;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 2;
    width: 12px;
  }
</style>
